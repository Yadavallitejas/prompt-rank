[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    scoring: Scoring engine tests
    rating: ELO rating engine tests
    anticheat: Anti-cheat module tests
    e2e: End-to-end integration tests (requires running services)
